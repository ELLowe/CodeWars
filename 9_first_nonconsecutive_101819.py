# Your task is to find the first element of an array that is not consecutive.
# If the whole array is consecutive then return null or Nothing.

def firstNonConsec(inputArray):
    for index,item in enumerate(inputArray):
        if (index == (len(inputArray)-1)
            return None;
        if (item+1 != inputArray[index+1]){
            return inputArray[i+1];
